plugins {
    id 'java'
}

group 'ru.bulldog.justchat.server'
version '1.0-SNAPSHOT'

dependencies {
    implementation project(':common')
    implementation group: 'org.xerial', name: 'sqlite-jdbc', version: '3.34.0'
}

test {
    useJUnitPlatform()
}